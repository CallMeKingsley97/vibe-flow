# 性能基线

基准场景：单会话 10,000 个事件、250 个任务阶段，包含消息与命令工具调用。

2026-07-15 本地基线：前端统计与流程聚合测试总耗时约 32 ms。CI 回归阈值分别设置为 1,500 ms，为不同平台和共享 Runner 保留充足波动空间。

自动回归测试：

```bash
pnpm test:performance
```

长事件明细使用虚拟列表；流程按每页 8 个阶段展示，并提供缩略导航和风险筛选。性能测试不等同于渲染帧率测试，发布前仍需进行真实长会话视觉验收。
